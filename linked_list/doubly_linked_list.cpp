/* In this program we'limplement doubly linked list
with the help of which we can move in either direction */

#include<bits/stdc++.h>
using namespace std;

struct Node
{
    struct Node *prev;
    int data;
    struct Node *next;
}*first=NULL;

int main()
{
    return 0;
}